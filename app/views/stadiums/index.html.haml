- breadcrumb :stadiums
- breadcrumb :category, @category  if @category
.body-bg
  .row
    .entry
      %h1
        Стадионы
      %hr/
      = render 'search_on_map', include_stadiums: true, q: @q
      %hr/
      = render 'map'
      %hr/
      %ul.list-unstyled.stadiums
        = render @stadiums
