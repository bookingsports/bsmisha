$('.calendar').fullCalendar('removeEvents', [<%= @group_event.id %>]);
$('.calendar').fullCalendar(
    'renderEvent',
    $.parseJSON("<%=j render(@group_event, format: :json).html_safe %>"),
    true
);
$('.modal').modal('hide');