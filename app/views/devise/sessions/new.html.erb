<% breadcrumb :account %>

<div class="body-bg">
  <div class="row">
    <div class="entry">
      <h1>Личный кабинет</h1>
      <hr>
      <div class="row">
        <div class="col-md-6">
          <div class="authform new_session">
            <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
              <h3>Авторизация</h3>
              <%= devise_error_messages! %>
              <div class="form-group">
                <%= f.label :email %>
                <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%- if devise_mapping.recoverable? %>
                  <%= link_to "Забыли пароль?", new_password_path(resource_name), class: 'right' %>
                <% end -%>
                <%= f.label :password %>
                <%= f.password_field :password, class: 'form-control' %>
              </div>
                <% if devise_mapping.rememberable? -%>
                    <div class="form-group check-wrap" style="width:150px">
                      <%= f.check_box :remember_me %>
                      <label class="check-label" for="user_remember_me">Запомнить меня</label>
                    </div>
                <% end -%>
                <%= f.submit 'Войти', :class => 'btn btn-success' %>
            <% end %>
          </div>
        </div>
        <div class="col-md-6">
          <%= render 'devise/registrations/form' %>
        </div>
      </div>
    </div>
  </div>
 </div>

