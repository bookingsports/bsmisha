= simple_form_for [:admin, @page] do |f|
  .form-inputs
    = f.input :title, label: 'Заголовок'
    = f.input :text, as: :bootsy, label: 'Содержимое'

  .form-actions
    = f.button :submit, 'Сохранить'
