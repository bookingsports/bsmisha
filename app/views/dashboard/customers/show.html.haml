%h3= @customer.name
%dl
  %dt Баланс
  %dd= number_to_integer_currency @customer.events.paid.where(coach: current_user.coach).map(&:price).inject(&:+).to_i
%h3 Грядущие
%table.table
  %thead
    %tr
      %th Стадион
      %th Начало
      %th Конец
      %th Сумма
  %tbody
    - @customer.events.future.paid.where(coach: current_user.coach).order(created_at: :desc).each do |e|
      %tr
        %td= e.area.stadium.name
        %td= l e.start, format: :short
        %td= l e.stop, format: :short
        %td= number_to_integer_currency e.price
%h3 Прошедшие
%table.table
  %thead
    %tr
      %th Стадион
      %th Начало
      %th Конец
      %th Сумма
  %tbody
    - @customer.events.past.paid.where(coach: current_user.coach).order(created_at: :desc).each do |e|
      %tr
        %td= e.product_names
        %td= l e.start, format: :short
        %td= l e.end, format: :short
        %td= number_to_integer_currency e.price